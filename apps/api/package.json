{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "swag init -g main.go -d ./cmd/api,./internal/handler,./internal/router -o docs && air",
    "build": "go build -o bin/api ./cmd/api"
  }
}