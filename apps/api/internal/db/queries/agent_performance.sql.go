// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: agent_performance.sql

package queries

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listLGASupervisorPerformanceStats = `-- name: ListLGASupervisorPerformanceStats :many
SELECT
  les.id,
  les.user_id,
  les.party_id,
  les.election_group_id,
  les.state_id,
  les.lga_id,
  0::int AS ward_id,
  0::int AS polling_unit_id,
  TRIM(CONCAT(COALESCE(u.first_name, ''), ' ', COALESCE(u.last_name, '')))::varchar AS user_name,
  COALESCE(u.avatar, '')::varchar AS avatar_url,
  les.role_type,
  100::numeric(5,2) AS readiness_pct,
  COALESCE(TO_CHAR(les.arrived_at, 'HH12:MI AM'), '')::varchar AS arrived_at,
  COALESCE(TO_CHAR(egl.pu_average_election_started_at, 'HH12:MI AM'), '')::varchar AS election_started_at,
  COALESCE(TO_CHAR(egl.pu_average_election_ended_at, 'HH12:MI AM'), '')::varchar AS election_ended_at,
  COALESCE(ae.total_earned_kobo, les.earned_amount_kobo, 0)::bigint AS earnings_kobo,
  CONCAT('₦', TO_CHAR(COALESCE(ae.total_earned_kobo, les.earned_amount_kobo, 0) / 100.0, 'FM999,999,999,990.00'))::varchar AS earnings_formatted,
  (les.completed_at IS NOT NULL)::boolean AS completion_status,
  COALESCE(ae.status = 'requested' OR ae.requested_at IS NOT NULL, false)::boolean AS requested_payout,
  COALESCE(ae.status = 'paid' OR ae.paid_at IS NOT NULL, false)::boolean AS paid,
  COALESCE(l.state_name, '')::varchar AS state_name,
  COALESCE(l.name, '')::varchar AS lga_name
FROM lga_election_supervisors les
JOIN users u ON u.id = les.user_id
LEFT JOIN lgas l ON l.id = les.lga_id
LEFT JOIN agent_earnings ae ON ae.user_id = les.user_id AND ae.election_group_id = les.election_group_id AND ae.role_type = les.role_type
LEFT JOIN election_group_lgas egl ON egl.election_group_id = les.election_group_id AND egl.lga_id = les.lga_id
WHERE
  ($1::smallint = 0 OR les.party_id = $1) AND
  ($2::bigint = 0 OR les.election_group_id = $2) AND
  ($3::smallint = 0 OR les.state_id = $3) AND
  ($4::int = 0 OR les.lga_id = $4) AND
  ($5::varchar = '' OR u.first_name ILIKE '%' || $5 || '%' OR u.last_name ILIKE '%' || $5 || '%') AND
  ($6::bigint = 0 OR les.id < $6)
ORDER BY les.id DESC
LIMIT $7::int
`

type ListLGASupervisorPerformanceStatsParams struct {
	PartyID         int16  `json:"party_id"`
	ElectionGroupID int64  `json:"election_group_id"`
	StateID         int16  `json:"state_id"`
	LgaID           int32  `json:"lga_id"`
	SearchQuery     string `json:"search_query"`
	CursorID        int64  `json:"cursor_id"`
	LimitVal        int32  `json:"limit_val"`
}

type ListLGASupervisorPerformanceStatsRow struct {
	ID                int64          `json:"id"`
	UserID            int64          `json:"user_id"`
	PartyID           int16          `json:"party_id"`
	ElectionGroupID   int64          `json:"election_group_id"`
	StateID           int16          `json:"state_id"`
	LgaID             int32          `json:"lga_id"`
	WardID            int32          `json:"ward_id"`
	PollingUnitID     int32          `json:"polling_unit_id"`
	UserName          string         `json:"user_name"`
	AvatarUrl         string         `json:"avatar_url"`
	RoleType          pgtype.Text    `json:"role_type"`
	ReadinessPct      pgtype.Numeric `json:"readiness_pct"`
	ArrivedAt         string         `json:"arrived_at"`
	ElectionStartedAt string         `json:"election_started_at"`
	ElectionEndedAt   string         `json:"election_ended_at"`
	EarningsKobo      int64          `json:"earnings_kobo"`
	EarningsFormatted string         `json:"earnings_formatted"`
	CompletionStatus  bool           `json:"completion_status"`
	RequestedPayout   bool           `json:"requested_payout"`
	Paid              bool           `json:"paid"`
	StateName         string         `json:"state_name"`
	LgaName           string         `json:"lga_name"`
}

func (q *Queries) ListLGASupervisorPerformanceStats(ctx context.Context, arg ListLGASupervisorPerformanceStatsParams) ([]ListLGASupervisorPerformanceStatsRow, error) {
	rows, err := q.db.Query(ctx, listLGASupervisorPerformanceStats,
		arg.PartyID,
		arg.ElectionGroupID,
		arg.StateID,
		arg.LgaID,
		arg.SearchQuery,
		arg.CursorID,
		arg.LimitVal,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListLGASupervisorPerformanceStatsRow
	for rows.Next() {
		var i ListLGASupervisorPerformanceStatsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.PartyID,
			&i.ElectionGroupID,
			&i.StateID,
			&i.LgaID,
			&i.WardID,
			&i.PollingUnitID,
			&i.UserName,
			&i.AvatarUrl,
			&i.RoleType,
			&i.ReadinessPct,
			&i.ArrivedAt,
			&i.ElectionStartedAt,
			&i.ElectionEndedAt,
			&i.EarningsKobo,
			&i.EarningsFormatted,
			&i.CompletionStatus,
			&i.RequestedPayout,
			&i.Paid,
			&i.StateName,
			&i.LgaName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPollingAgentPerformanceStats = `-- name: ListPollingAgentPerformanceStats :many
SELECT
  pua.id,
  pua.user_id,
  pua.party_id,
  pua.election_group_id,
  COALESCE(pu.state_id, 0)::smallint AS state_id,
  COALESCE(pu.lga_id, 0)::int AS lga_id,
  COALESCE(pu.ward_id, 0)::int AS ward_id,
  pua.polling_unit_id,
  TRIM(CONCAT(COALESCE(u.first_name, ''), ' ', COALESCE(u.last_name, '')))::varchar AS user_name,
  COALESCE(u.avatar, '')::varchar AS avatar_url,
  pua.role_type,
  pua.election_practice_test_readiness_percentage::numeric(5,2) AS readiness_pct,
  COALESCE(TO_CHAR(pua.arrived_at, 'HH12:MI AM'), '')::varchar AS arrived_at,
  COALESCE(TO_CHAR(pua.election_started_at, 'HH12:MI AM'), '')::varchar AS election_started_at,
  COALESCE(TO_CHAR(pua.election_ended_at, 'HH12:MI AM'), '')::varchar AS election_ended_at,
  pua.updates_count AS updates_given,
  pua.reports_count AS reports_given,
  pua.live_voters_referred_count AS live_voters_referred,
  CONCAT(pua.results_submitted_count, '/', COALESCE(egpu.unique_final_results_expected, 1))::varchar AS results_uploaded,
  COALESCE(ae.total_earned_kobo, pua.earned_amount_kobo, 0)::bigint AS earnings_kobo,
  CONCAT('₦', TO_CHAR(COALESCE(ae.total_earned_kobo, pua.earned_amount_kobo, 0) / 100.0, 'FM999,999,999,990.00'))::varchar AS earnings_formatted,
  (pua.completed_at IS NOT NULL)::boolean AS completion_status,
  COALESCE(ae.status = 'requested' OR ae.requested_at IS NOT NULL, false)::boolean AS requested_payout,
  COALESCE(ae.status = 'paid' OR ae.paid_at IS NOT NULL, false)::boolean AS paid,
  COALESCE(pu.state_name, '')::varchar AS state_name,
  COALESCE(pu.lga_name, '')::varchar AS lga_name,
  COALESCE(pu.ward_name, '')::varchar AS ward_name,
  COALESCE(pu.name, '')::varchar AS polling_unit_name,
  COALESCE(pu.pu_code, pu.code, '')::varchar AS polling_unit_code
FROM polling_unit_assignments pua
JOIN users u ON u.id = pua.user_id
LEFT JOIN polling_units pu ON pu.id = pua.polling_unit_id
LEFT JOIN agent_earnings ae ON ae.user_id = pua.user_id AND ae.election_group_id = pua.election_group_id AND ae.role_type = pua.role_type
LEFT JOIN election_group_polling_units egpu ON egpu.election_group_id = pua.election_group_id AND egpu.polling_unit_id = pua.polling_unit_id
WHERE
  ($1::smallint = 0 OR pua.party_id = $1) AND
  ($2::bigint = 0 OR pua.election_group_id = $2) AND
  ($3::smallint = 0 OR pu.state_id = $3) AND
  ($4::int = 0 OR pu.lga_id = $4) AND
  ($5::int = 0 OR pu.ward_id = $5) AND
  ($6::varchar = '' OR u.first_name ILIKE '%' || $6 || '%' OR u.last_name ILIKE '%' || $6 || '%' OR pu.pu_code ILIKE '%' || $6 || '%' OR pu.code ILIKE '%' || $6 || '%') AND
  ($7::bigint = 0 OR pua.id < $7)
ORDER BY pua.id DESC
LIMIT $8::int
`

type ListPollingAgentPerformanceStatsParams struct {
	PartyID         int16  `json:"party_id"`
	ElectionGroupID int64  `json:"election_group_id"`
	StateID         int16  `json:"state_id"`
	LgaID           int32  `json:"lga_id"`
	WardID          int32  `json:"ward_id"`
	SearchQuery     string `json:"search_query"`
	CursorID        int64  `json:"cursor_id"`
	LimitVal        int32  `json:"limit_val"`
}

type ListPollingAgentPerformanceStatsRow struct {
	ID                 int64          `json:"id"`
	UserID             int64          `json:"user_id"`
	PartyID            int16          `json:"party_id"`
	ElectionGroupID    int64          `json:"election_group_id"`
	StateID            int16          `json:"state_id"`
	LgaID              int32          `json:"lga_id"`
	WardID             int32          `json:"ward_id"`
	PollingUnitID      int32          `json:"polling_unit_id"`
	UserName           string         `json:"user_name"`
	AvatarUrl          string         `json:"avatar_url"`
	RoleType           pgtype.Text    `json:"role_type"`
	ReadinessPct       pgtype.Numeric `json:"readiness_pct"`
	ArrivedAt          string         `json:"arrived_at"`
	ElectionStartedAt  string         `json:"election_started_at"`
	ElectionEndedAt    string         `json:"election_ended_at"`
	UpdatesGiven       int32          `json:"updates_given"`
	ReportsGiven       int32          `json:"reports_given"`
	LiveVotersReferred int32          `json:"live_voters_referred"`
	ResultsUploaded    string         `json:"results_uploaded"`
	EarningsKobo       int64          `json:"earnings_kobo"`
	EarningsFormatted  string         `json:"earnings_formatted"`
	CompletionStatus   bool           `json:"completion_status"`
	RequestedPayout    bool           `json:"requested_payout"`
	Paid               bool           `json:"paid"`
	StateName          string         `json:"state_name"`
	LgaName            string         `json:"lga_name"`
	WardName           string         `json:"ward_name"`
	PollingUnitName    string         `json:"polling_unit_name"`
	PollingUnitCode    string         `json:"polling_unit_code"`
}

func (q *Queries) ListPollingAgentPerformanceStats(ctx context.Context, arg ListPollingAgentPerformanceStatsParams) ([]ListPollingAgentPerformanceStatsRow, error) {
	rows, err := q.db.Query(ctx, listPollingAgentPerformanceStats,
		arg.PartyID,
		arg.ElectionGroupID,
		arg.StateID,
		arg.LgaID,
		arg.WardID,
		arg.SearchQuery,
		arg.CursorID,
		arg.LimitVal,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListPollingAgentPerformanceStatsRow
	for rows.Next() {
		var i ListPollingAgentPerformanceStatsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.PartyID,
			&i.ElectionGroupID,
			&i.StateID,
			&i.LgaID,
			&i.WardID,
			&i.PollingUnitID,
			&i.UserName,
			&i.AvatarUrl,
			&i.RoleType,
			&i.ReadinessPct,
			&i.ArrivedAt,
			&i.ElectionStartedAt,
			&i.ElectionEndedAt,
			&i.UpdatesGiven,
			&i.ReportsGiven,
			&i.LiveVotersReferred,
			&i.ResultsUploaded,
			&i.EarningsKobo,
			&i.EarningsFormatted,
			&i.CompletionStatus,
			&i.RequestedPayout,
			&i.Paid,
			&i.StateName,
			&i.LgaName,
			&i.WardName,
			&i.PollingUnitName,
			&i.PollingUnitCode,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listStateSupervisorPerformanceStats = `-- name: ListStateSupervisorPerformanceStats :many
SELECT
  ses.id,
  ses.user_id,
  ses.party_id,
  ses.election_group_id,
  ses.state_id,
  0::int AS lga_id,
  0::int AS ward_id,
  0::int AS polling_unit_id,
  TRIM(CONCAT(COALESCE(u.first_name, ''), ' ', COALESCE(u.last_name, '')))::varchar AS user_name,
  COALESCE(u.avatar, '')::varchar AS avatar_url,
  ses.role_type,
  100::numeric(5,2) AS readiness_pct,
  COALESCE(TO_CHAR(ses.arrived_at, 'HH12:MI AM'), '')::varchar AS arrived_at,
  COALESCE(TO_CHAR(egs.pu_average_election_started_at, 'HH12:MI AM'), '')::varchar AS election_started_at,
  COALESCE(TO_CHAR(egs.pu_average_election_ended_at, 'HH12:MI AM'), '')::varchar AS election_ended_at,
  COALESCE(ae.total_earned_kobo, ses.earned_amount_kobo, 0)::bigint AS earnings_kobo,
  CONCAT('₦', TO_CHAR(COALESCE(ae.total_earned_kobo, ses.earned_amount_kobo, 0) / 100.0, 'FM999,999,999,990.00'))::varchar AS earnings_formatted,
  (ses.completed_at IS NOT NULL)::boolean AS completion_status,
  COALESCE(ae.status = 'requested' OR ae.requested_at IS NOT NULL, false)::boolean AS requested_payout,
  COALESCE(ae.status = 'paid' OR ae.paid_at IS NOT NULL, false)::boolean AS paid,
  COALESCE(s.name, '')::varchar AS state_name
FROM state_election_supervisors ses
JOIN users u ON u.id = ses.user_id
LEFT JOIN c_states s ON s.id = ses.state_id
LEFT JOIN agent_earnings ae ON ae.user_id = ses.user_id AND ae.election_group_id = ses.election_group_id AND ae.role_type = ses.role_type
LEFT JOIN election_group_states egs ON egs.election_group_id = ses.election_group_id AND egs.state_id = ses.state_id
WHERE
  ($1::smallint = 0 OR ses.party_id = $1) AND
  ($2::bigint = 0 OR ses.election_group_id = $2) AND
  ($3::smallint = 0 OR ses.state_id = $3) AND
  ($4::varchar = '' OR u.first_name ILIKE '%' || $4 || '%' OR u.last_name ILIKE '%' || $4 || '%') AND
  ($5::bigint = 0 OR ses.id < $5)
ORDER BY ses.id DESC
LIMIT $6::int
`

type ListStateSupervisorPerformanceStatsParams struct {
	PartyID         int16  `json:"party_id"`
	ElectionGroupID int64  `json:"election_group_id"`
	StateID         int16  `json:"state_id"`
	SearchQuery     string `json:"search_query"`
	CursorID        int64  `json:"cursor_id"`
	LimitVal        int32  `json:"limit_val"`
}

type ListStateSupervisorPerformanceStatsRow struct {
	ID                int64          `json:"id"`
	UserID            int64          `json:"user_id"`
	PartyID           int16          `json:"party_id"`
	ElectionGroupID   int64          `json:"election_group_id"`
	StateID           int16          `json:"state_id"`
	LgaID             int32          `json:"lga_id"`
	WardID            int32          `json:"ward_id"`
	PollingUnitID     int32          `json:"polling_unit_id"`
	UserName          string         `json:"user_name"`
	AvatarUrl         string         `json:"avatar_url"`
	RoleType          pgtype.Text    `json:"role_type"`
	ReadinessPct      pgtype.Numeric `json:"readiness_pct"`
	ArrivedAt         string         `json:"arrived_at"`
	ElectionStartedAt string         `json:"election_started_at"`
	ElectionEndedAt   string         `json:"election_ended_at"`
	EarningsKobo      int64          `json:"earnings_kobo"`
	EarningsFormatted string         `json:"earnings_formatted"`
	CompletionStatus  bool           `json:"completion_status"`
	RequestedPayout   bool           `json:"requested_payout"`
	Paid              bool           `json:"paid"`
	StateName         string         `json:"state_name"`
}

func (q *Queries) ListStateSupervisorPerformanceStats(ctx context.Context, arg ListStateSupervisorPerformanceStatsParams) ([]ListStateSupervisorPerformanceStatsRow, error) {
	rows, err := q.db.Query(ctx, listStateSupervisorPerformanceStats,
		arg.PartyID,
		arg.ElectionGroupID,
		arg.StateID,
		arg.SearchQuery,
		arg.CursorID,
		arg.LimitVal,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListStateSupervisorPerformanceStatsRow
	for rows.Next() {
		var i ListStateSupervisorPerformanceStatsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.PartyID,
			&i.ElectionGroupID,
			&i.StateID,
			&i.LgaID,
			&i.WardID,
			&i.PollingUnitID,
			&i.UserName,
			&i.AvatarUrl,
			&i.RoleType,
			&i.ReadinessPct,
			&i.ArrivedAt,
			&i.ElectionStartedAt,
			&i.ElectionEndedAt,
			&i.EarningsKobo,
			&i.EarningsFormatted,
			&i.CompletionStatus,
			&i.RequestedPayout,
			&i.Paid,
			&i.StateName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listWardSupervisorPerformanceStats = `-- name: ListWardSupervisorPerformanceStats :many
SELECT
  wes.id,
  wes.user_id,
  wes.party_id,
  wes.election_group_id,
  wes.state_id,
  wes.lga_id,
  wes.ward_id,
  0::int AS polling_unit_id,
  TRIM(CONCAT(COALESCE(u.first_name, ''), ' ', COALESCE(u.last_name, '')))::varchar AS user_name,
  COALESCE(u.avatar, '')::varchar AS avatar_url,
  wes.role_type,
  100::numeric(5,2) AS readiness_pct,
  COALESCE(TO_CHAR(wes.arrived_at, 'HH12:MI AM'), '')::varchar AS arrived_at,
  COALESCE(TO_CHAR(egw.pu_average_election_started_at, 'HH12:MI AM'), '')::varchar AS election_started_at,
  COALESCE(TO_CHAR(egw.pu_average_election_ended_at, 'HH12:MI AM'), '')::varchar AS election_ended_at,
  COALESCE(ae.total_earned_kobo, wes.earned_amount_kobo, 0)::bigint AS earnings_kobo,
  CONCAT('₦', TO_CHAR(COALESCE(ae.total_earned_kobo, wes.earned_amount_kobo, 0) / 100.0, 'FM999,999,999,990.00'))::varchar AS earnings_formatted,
  (wes.completed_at IS NOT NULL)::boolean AS completion_status,
  COALESCE(ae.status = 'requested' OR ae.requested_at IS NOT NULL, false)::boolean AS requested_payout,
  COALESCE(ae.status = 'paid' OR ae.paid_at IS NOT NULL, false)::boolean AS paid,
  COALESCE(w.state_name, '')::varchar AS state_name,
  COALESCE(w.lga_name, '')::varchar AS lga_name,
  COALESCE(w.name, '')::varchar AS ward_name
FROM ward_election_supervisors wes
JOIN users u ON u.id = wes.user_id
LEFT JOIN wards w ON w.id = wes.ward_id
LEFT JOIN agent_earnings ae ON ae.user_id = wes.user_id AND ae.election_group_id = wes.election_group_id AND ae.role_type = wes.role_type
LEFT JOIN election_group_wards egw ON egw.election_group_id = wes.election_group_id AND egw.ward_id = wes.ward_id
WHERE
  ($1::smallint = 0 OR wes.party_id = $1) AND
  ($2::bigint = 0 OR wes.election_group_id = $2) AND
  ($3::smallint = 0 OR wes.state_id = $3) AND
  ($4::int = 0 OR wes.lga_id = $4) AND
  ($5::int = 0 OR wes.ward_id = $5) AND
  ($6::varchar = '' OR u.first_name ILIKE '%' || $6 || '%' OR u.last_name ILIKE '%' || $6 || '%') AND
  ($7::bigint = 0 OR wes.id < $7)
ORDER BY wes.id DESC
LIMIT $8::int
`

type ListWardSupervisorPerformanceStatsParams struct {
	PartyID         int16  `json:"party_id"`
	ElectionGroupID int64  `json:"election_group_id"`
	StateID         int16  `json:"state_id"`
	LgaID           int32  `json:"lga_id"`
	WardID          int32  `json:"ward_id"`
	SearchQuery     string `json:"search_query"`
	CursorID        int64  `json:"cursor_id"`
	LimitVal        int32  `json:"limit_val"`
}

type ListWardSupervisorPerformanceStatsRow struct {
	ID                int64          `json:"id"`
	UserID            int64          `json:"user_id"`
	PartyID           int16          `json:"party_id"`
	ElectionGroupID   int64          `json:"election_group_id"`
	StateID           int16          `json:"state_id"`
	LgaID             int32          `json:"lga_id"`
	WardID            int32          `json:"ward_id"`
	PollingUnitID     int32          `json:"polling_unit_id"`
	UserName          string         `json:"user_name"`
	AvatarUrl         string         `json:"avatar_url"`
	RoleType          pgtype.Text    `json:"role_type"`
	ReadinessPct      pgtype.Numeric `json:"readiness_pct"`
	ArrivedAt         string         `json:"arrived_at"`
	ElectionStartedAt string         `json:"election_started_at"`
	ElectionEndedAt   string         `json:"election_ended_at"`
	EarningsKobo      int64          `json:"earnings_kobo"`
	EarningsFormatted string         `json:"earnings_formatted"`
	CompletionStatus  bool           `json:"completion_status"`
	RequestedPayout   bool           `json:"requested_payout"`
	Paid              bool           `json:"paid"`
	StateName         string         `json:"state_name"`
	LgaName           string         `json:"lga_name"`
	WardName          string         `json:"ward_name"`
}

func (q *Queries) ListWardSupervisorPerformanceStats(ctx context.Context, arg ListWardSupervisorPerformanceStatsParams) ([]ListWardSupervisorPerformanceStatsRow, error) {
	rows, err := q.db.Query(ctx, listWardSupervisorPerformanceStats,
		arg.PartyID,
		arg.ElectionGroupID,
		arg.StateID,
		arg.LgaID,
		arg.WardID,
		arg.SearchQuery,
		arg.CursorID,
		arg.LimitVal,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListWardSupervisorPerformanceStatsRow
	for rows.Next() {
		var i ListWardSupervisorPerformanceStatsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.PartyID,
			&i.ElectionGroupID,
			&i.StateID,
			&i.LgaID,
			&i.WardID,
			&i.PollingUnitID,
			&i.UserName,
			&i.AvatarUrl,
			&i.RoleType,
			&i.ReadinessPct,
			&i.ArrivedAt,
			&i.ElectionStartedAt,
			&i.ElectionEndedAt,
			&i.EarningsKobo,
			&i.EarningsFormatted,
			&i.CompletionStatus,
			&i.RequestedPayout,
			&i.Paid,
			&i.StateName,
			&i.LgaName,
			&i.WardName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
