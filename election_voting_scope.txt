Next up: if yes is clicked on [DidYouVoteCard.tsx#L13-19](textBlock;file:///c%3A/Users/danie/Desktop/Repos/free9ja/apps/election-web/src/routes/_authenticated/home/components/DidYouVoteCard.tsx#L13-19)  .

Then we want to navigate to a page where we carryout the voting process, for the selected election group's elections. knowing that an election group may have 1 or more elections, that only means for an election group for a specific polling unit, we can vote for 1 or more election with this voting form.

Now the elections we vote for in the selected election group is totally dependent on the elections scope

if scope = nationwide:
- every polling unit can vote for that election

if scope = state:
- every polling unit in that state can vote for it (our submitted polling unit state must match election state)

if scope = senatorial-district:
- every polling unit in that senatorial district can vote in that election (our submitted polling unit.lga.senatorial_district_id must match election.senatorial_district_id)

if scope = federal-constituency:
- every polling unit in that federal-constituency can vote in that election (our submitted polling unit.lga.federal_constituency_id must match election.federal_constituency_id)

if scope = lga:
- every polling unit in that lga can vote in that election (our submitted lga_id must match election.lga_id)

if scope = state-constituency:
- every polling unit in that state-constituency can vote in that election (our submitted polling unit.ward.state_assembly_constituency_id must match election.state_constituency_id)

if scope = ward:
- every polling unit in that ward can vote in that election (our submitted ward_id must match election.lga_id)

the voting flow can take on the examplary flow and structure used here [apply.tsx#L0-522](textBlock;file:///c%3A/Users/danie/Desktop/Repos/free9ja/apps/election-web/src/routes/_authenticated/applications/apply.tsx#L0-522) 

Elections fetched based on the election group that should be fetched so the user can vote should only be elections allowed to hold in that polling unit

